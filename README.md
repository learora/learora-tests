# learora-tests

Standalone, no-account reading/ranking tests for Learora's corpus work.
Each subfolder is one test, served as a plain static page via GitHub Pages.

- `/sorting-room/` — separation test: rank real cards against seeded encyclopedia paragraphs.
